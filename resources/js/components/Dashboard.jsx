import React, { StrictMode } from 'react';  // Asegúrate de importar StrictMode desde React
import SecondarySidebar from './dashborad';

const Dashboard = () => {
  return (
   <SecondarySidebar/>
  );
};

export default Dashboard;
